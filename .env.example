# Anthropic API key — required. Get one at https://console.anthropic.com/
# Never commit your real .env file (it is gitignored).
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: port to bind locally. Railway sets PORT automatically.
PORT=3000
