---
description: Load one Linear ticket by identifier (e.g. EMP-123) or title and summarize it with the gaps a spec must close
---

You are loading a single Linear ticket in full.

## Procedure

1. **Read** `ai/skills/read_linear_board.md` (Task mode). Follow it.
2. Resolve `$ARGUMENTS` (identifier or title fragment); disambiguate if more than one match.
3. Summarize the ticket and list the gaps/ambiguities a spec will need to resolve.
4. Suggest `/sdd-spec-from-ticket <id>`.

Read-only. Do not modify the ticket.
